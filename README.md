# Stream announcement for TWITCH

## Install

+ Edit and set variables in ```config.py```
+ Install dependencies:
```
$ pip install -r requirements.txt
```
+ Run ```app.py```:
```
$ python3 app.py
```
